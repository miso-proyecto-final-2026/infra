apiVersion: v1
kind: Service
metadata:
  name: mock-open-finance
  namespace: solventa-staging
spec:
  selector:
    app: mock-open-finance
  ports:
    - port: 8000
      targetPort: 8000
  type: ClusterIP
